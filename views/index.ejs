<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>MindCare - Find Calm, One Breath at a Time</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
  <link rel="stylesheet" href="/css/style.css" />
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@500;700&family=Open+Sans&display=swap" rel="stylesheet" />
</head>
<body>
  <%- include('partials/navbar') %>

  <% if (message) { %>
  <div class="alert alert-success alert-dismissible fade show m-3" role="alert">
    <%= message %>
    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
  </div>
  <% } %>

  <header class="hero d-flex align-items-center text-center text-white">
    <div class="container animate-fade-in">
      <h1 class="hero-title">MindCare</h1>
      <p class="hero-subtitle">Find calm, one breath at a time</p>
      <a href="#exercises" class="btn btn-light btn-lg mt-3 hover-lift">Start Now</a>
    </div>
  </header>

  <section id="stats" class="py-5 bg-light">
    <div class="container">
      <div class="row text-center">
        <div class="row row-cols-1 row-cols-md-3 g-4 equal-cols">
          <div class="col">
            <div class="stat-card card-3d animate-on-scroll h-100">
              <i class="bi bi-clock-history text-primary"></i>
              <h3><span data-count="<%= stats.totalSessions %>"><%= stats.totalSessions %></span></h3>
              <p class="text-muted">Total Sessions</p>
            </div>
          </div>

          <div class="col">
            <div class="stat-card card-3d animate-on-scroll h-100">
              <i class="bi bi-hourglass-split text-success"></i>
              <h3><span data-count="<%= stats.totalMinutes %>"><%= stats.totalMinutes %></span> min</h3>
              <p class="text-muted">Time Practiced</p>
            </div>
          </div>

          <div class="col">
            <div class="stat-card card-3d animate-on-scroll h-100">
              <i class="bi bi-fire text-danger"></i>
              <h3><span data-count="<%= stats.currentStreak %>"><%= stats.currentStreak %></span> days</h3>
              <p class="text-muted">Current Streak</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section id="why" class="py-5 bg-white">
    <div class="container text-center">
      <h2 class="section-title">Why MindCare?</h2>
      <p class="lead">We help you build daily habits that reduce stress, improve focus, and restore balance. No pressure—just breath.</p>
      <div class="row mt-5">
        <div class="col-md-4 mb-4">
          <div class="feature-card hover-lift">
            <i class="bi bi-heart-pulse display-4 text-danger mb-3"></i>
            <h5>Reduce Stress</h5>
            <p>Scientific breathing techniques to calm your nervous system</p>
          </div>
        </div>
        <div class="col-md-4 mb-4">
          <div class="feature-card hover-lift">
            <i class="bi bi-graph-up display-4 text-primary mb-3"></i>
            <h5>Track Progress</h5>
            <p>Monitor your journey and see your improvements over time</p>
          </div>
        </div>
        <div class="col-md-4 mb-4">
          <div class="feature-card hover-lift">
            <i class="bi bi-people display-4 text-success mb-3"></i>
            <h5>Expert Resources</h5>
            <p>Access curated materials from mental health professionals</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section id="exercises" class="py-5">
    <div class="container">
      <h2 class="text-center mb-5 section-title">Breathing Exercises</h2>
      <div class="row">
        <div class="col-md-4 mb-4">
          <div class="card exercise-card shadow hover-lift">
            <div class="exercise-icon">
              <i class="bi bi-wind"></i>
            </div>
            <div class="card-body text-center">
              <h5 class="card-title">4-7-8 Breathing</h5>
              <p class="card-text">Inhale for 4 seconds, hold for 7, exhale for
              <a href="/exercises#four-seven-eight" class="btn btn-primary">Try Now</a>
            </div>
          </div>
        </div>
        <div class="col-md-4 mb-4">
          <div class="card exercise-card shadow hover-lift">
            <div class="exercise-icon">
              <i class="bi bi-square"></i>
            </div>
            <div class="card-body text-center">
              <h5 class="card-title">Box Breathing</h5>
              <p class="card-text">Breathe in 4, hold 4, exhale 4, hold 4. A method used by athletes and professionals.</p>
              <a href="/exercises#box" class="btn btn-primary">Try Now</a>
            </div>
          </div>
        </div>
        <div class="col-md-4 mb-4">
          <div class="card exercise-card shadow hover-lift">
            <div class="exercise-icon">
              <i class="bi bi-lungs"></i>
            </div>
            <div class="card-body text-center">
              <h5 class="card-title">Deep Breathing</h5>
              <p class="card-text">Slow deep breaths to oxygenate your body and release stress naturally.</p>
              <a href="/exercises#deep" class="btn btn-primary">Try Now</a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <% if (recentSessions.length > 0) { %>
  <section id="recent" class="py-5 bg-light">
    <div class="container">
      <h2 class="text-center mb-4 section-title">Recent Sessions</h2>
      <div class="row">
        <% recentSessions.slice(0, 3).forEach(session => { %>
        <div class="col-md-4 mb-3">
          <div class="recent-session-card p-3">
            <div class="d-flex justify-content-between align-items-center">
              <h6><%= session.exerciseType %></h6>
              <span class="badge bg-primary"><%= session.duration %> min</span>
            </div>
            <p class="text-muted mb-0">
              <i class="bi bi-emoji-smile"></i> Mood: <%= session.mood %>
            </p>
            <small class="text-muted"><%= new Date(session.createdAt).toLocaleDateString() %></small>
          </div>
        </div>
        <% }); %>
      </div>
      <div class="text-center mt-3">
        <a href="/tracker" class="btn btn-outline-primary">View All Sessions</a>
      </div>
    </div>
  </section>
  <% } %>

  <%- include('partials/footer') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/js/main.js"></script>
</body>
</html>